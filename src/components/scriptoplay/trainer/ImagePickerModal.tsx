interface ImagePickerModalProps {
  onSelect: (url: string, name: string) => void;
  onClose: () => void;
}

export default function ImagePickerModal(_props: ImagePickerModalProps) {
  return null;
}
